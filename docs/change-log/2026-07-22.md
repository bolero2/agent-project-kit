# 2026-07-22

전제: `e6fe4b4` 이후, 킷 루트에서 작업.

## [ADD] bootstrap.sh `--diff` 모드

- 문제: no-clobber 설계라 킷 개선을 **이미 부트스트랩된 프로젝트로 전파할 경로가 없었음**
  (역수출의 반대 방향 부재).
- 동작: 동기화 대상(`.claude/` — 복사 모드와 동일한 파일 목록 함수 공유)은 `cmp`로 내용
  비교 후 다른 파일만 unified diff(`-` = 대상 현재 / `+` = 킷 버전) 표시. 프로젝트 소유
  파일(CLAUDE.md, .gitignore, docs/change-log/README.md)은 채워지며 달라지는 게 정상이므로
  존재만 확인. **아무것도 수정하지 않음.** 차이/누락 있으면 exit 1, 완전 동일이면 exit 0.
- 실측 테스트(킷 루트에서 실행, 전부 통과):
  ```bash
  TARGET=$(mktemp -d) && git -C "$TARGET" init -q && ./bootstrap.sh "$TARGET" > /dev/null
  ./bootstrap.sh --diff "$TARGET"; echo $?    # 동기화 4파일 "동일" + 소유 3파일 "있음" → 0
  echo '{}' > "$TARGET/.claude/settings.json"
  ./bootstrap.sh --diff "$TARGET"; echo $?    # settings.json "다름" + unified diff → 1
  ./bootstrap.sh --diff "$(mktemp -d)"; echo $?  # 전부 "없음" 7건 → 1
  ./bootstrap.sh --diff; echo $?              # 사용법 출력 → 1
  rm -rf "$TARGET"
  ```
  검증 앵커: 깨끗한 대상 = "동일" 4 + "있음" 3, exit 0. 변조 시 "다름" 1, exit 1.
- 복사 모드 회귀 확인: 신규 대상 생성 파일 7개로 기존과 동일(`bash -n` 구문 검사 포함).
- 파일: `bootstrap.sh`, `README.md`(사용법·운영 원칙), `GETTING-STARTED.md`(Phase 3)

## [ADD] 안전 훅 2종 이식 (외부 킷 아이디어 채용, 재작성)

- 출처 아이디어: `TheDecipherist/claude-code-mastery-project-starter-kit`의
  block-dangerous-bash.py / verify-no-secrets.sh. **그대로 복사하지 않고 재작성**하며
  원본 결함 4건 수정:
  ① rm 분리 플래그(`rm -r -f`) 미탐지 → 토큰 분석으로 결합/분리/순서 무관 탐지
  ② 시크릿 내용 검사가 워킹트리 파일 대상 → 인덱스 기준(`git show :파일`)으로 수정
  ③ Stop 훅 무한루프 가드(`stop_hook_active`) 부재 → 추가
  ④ `DROP TABLE` 검사가 grep 등 일반 명령까지 오탐 → DB 클라이언트 동시 존재 조건 추가
- `settings.json`에 연결: PreToolUse(Bash) + Stop, `${CLAUDE_PROJECT_DIR:-.}` 기준 경로.
- 실측 결과:
  - 차단 훅 21케이스(차단 12 / 통과 7 / 엣지 2) 전부 기대 일치.
  - 시크릿 훅: 민감 파일명·4개 내용 패턴 차단, `.env.example` 허용, 인덱스-기준 A/B
    (스테이징 깨끗+워킹트리 비밀 → 통과 / 반대 → 차단), 루프 가드, 킷 파일 전체
    스테이징 시 오탐 없음.
  - **작성 직후 이 세션에서 훅이 실제 활성화되어 `rm -rf` 포함 명령을 차단 —
    Claude Code 연동 end-to-end 실측 확인.**
- 검증 앵커 갱신(정정): bootstrap 신규 생성 7 → **9파일**, `--diff` 동기화 대상 4 → **6파일**.
- 재현:
  ```bash
  printf '{"tool_name":"Bash","tool_input":{"command":"rm -r -f x"}}' \
    | python3 .claude/hooks/block-dangerous-bash.py; echo $?   # 차단 메시지 + exit 2
  ```
- 트레이드오프: 훅 활성 프로젝트에서 에이전트는 `rm -rf` 사용 불가 → `-f` 없이 `rm -r`.
  `__pycache__`는 bootstrap 복사 제외 + 킷 `.gitignore`에 추가.
- 파일: `.claude/hooks/`(신규 2), `.claude/settings.json`, `bootstrap.sh`, `.gitignore`,
  `README.md`

## [ADD] 기존 프로젝트 편입(adopt) 플로우

- 배경: 사용자 요구 — 이미 Claude Code로 진행 중인 프로젝트를 하네스 체계로 편입.
  no-clobber 설계상 기존 CLAUDE.md·.gitignore·settings.json이 스킵되는 병합 3지점을 해결.
- 설계: 기계적 병합은 스크립트, 판단이 필요한 병합은 스킬로 분리(킷 원칙).
  - `bootstrap.sh --adopt <대상>`: git 저장소 + 클린 트리 요구(추적 파일 기준, 언트래킹
    허용 — HEAD가 롤백 지점). no-clobber 복사 + 기존 .gitignore에 **누락된 보안 패턴만**
    append(정확한 줄 일치 검사, 마커 주석 1회). 모든 변경은 미커밋으로 남김.
  - `/kit-adopt` 스킬: 기존 CLAUDE.md에 §0 삽입 + 기존 내용을 §1~9로 재배치(원문 보존,
    충돌 규칙은 사용자 결정), 기존 settings.json에 deny·훅 병합, §5-보안 대조,
    git log 기반 §3·§8 후보 제안. 커밋은 사용자 승인으로만.
  - claude-md-init 스킬에 상호 참조 추가(손으로 쓴 CLAUDE.md가 있으면 /kit-adopt 안내).
- 실측 테스트(전부 통과): 기존 CLAUDE.md·.gitignore 보존 + 누락 6패턴만 append(.env 중복
  없음) / 더러운 트리 거부(exit 1) / 재실행 멱등(생성·append 없음, 마커 중복 없음) /
  언트래킹만 있으면 허용 / 비-git 거부 / 복사 모드 회귀 10파일 / --diff 동일 7파일 exit 0.
- 검증 앵커 갱신(정정): bootstrap 신규 생성 9 → **10파일**, `--diff` 동기화 대상 6 → **7파일**.
- 미검증: `/kit-adopt` 스킬의 대화형 실행(CLAUDE.md 재편 품질)은 실제 프로젝트 편입 시
  검증 예정 — A 프로젝트가 첫 실전 대상.
- 파일: `bootstrap.sh`, `.claude/skills/kit-adopt/SKILL.md`(신규),
  `.claude/skills/claude-md-init/SKILL.md`, `README.md`, `GETTING-STARTED.md`
