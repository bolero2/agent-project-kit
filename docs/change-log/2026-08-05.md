# 2026-08-05

전제: `fe1aea2` 이후, 킷 루트에서 작업.

## [UPDATE] 킷 소유 파일 무수정 규칙 명문화

- 배경: "킷 스킬은 재설치로만 갱신"이 skill-sync 스킬 안에만 있고, Agent가 읽는
  AGENT-RULES/CONTEXT에는 stage/commit 금지만 있어 설치된 스킬·Agent 정의를 에이전트가
  선의로 "개선"할 여지가 있었음.
- 수정: AGENT-RULES에 "킷 소유 파일 무수정" 절 신설(수정 금지, 개선은 보고 → 킷 저장소
  반영 → 재설치, 예외는 mutable CONTEXT/HANDOFF), CONTEXT 공통 규칙의 하네스 파일 항목에
  "수정도 하지 않는다" 명시.
- 검증: Agent payload·schema 테스트 통과. payload 내용 변경만이라 schema bump 없음.
- 파일: `payload/runtime/AGENT-RULES.md`, `payload/runtime/CONTEXT.md`

## [FIX] Jira 번호 금지 규칙 과대해석 정정 + soln-va-tools 경유 의무 강화 (실측 발견)

- 관측: PR #182(soln-agentic-dataforge)가 create-pr 스킬 템플릿·`[DF-810]` 제목 prefix 없이
  생성됨. 원인은 킷 규칙 결함 — 사용자 원 지시는 "브랜치 네이밍에만 Jira 번호 금지"였는데
  AGENT-RULES에 "브랜치·커밋·PR 제목 전부 금지"로 과대해석되어 기록됐고, 이 규칙이
  create-pr 스킬의 PR 제목 prefix 규약과 충돌해 Agent가 스킬을 회피함.
- 수정:
  - Jira 번호 금지를 **브랜치명 한정**으로 정정(`feat/<slug>`, `fix/<slug>` 네이밍).
    커밋·PR 제목·description은 스킬/프로젝트 규약을 따르며, create-pr의 `[DF-XXX]` PR 제목
    prefix는 정상 규약으로 명시.
  - 스킬 경유 의무 강화: soln-va-tools가 설치되어 있으면 commit/push/PR에 반드시 경유,
    스킬이 있는데 직접 `git commit`/`gh pr create` 실행 금지, 부재 확인 시에만 자체 수행.
    스킬 출력 규약 변조 금지.
  - 커밋 분할 기준 보강: 최소 기능 단위, 필요하면 더 쪼갬.
  - 반영 파일: AGENT-RULES, developer AGENT.md, README, GETTING-STARTED,
    acceptance-test(I 실패 기준에 "스킬 미경유 commit/PR" 추가), Confluence 3번 페이지.
- 검증: 전체 83개 테스트 통과. 실전 재검증(스킬 경유 + prefix 확인)은 대상 저장소 재설치 후.

## [FIX] developer 착수 시 "진행 중" 이동 누락 (실전 재검증 결과 반영)

- 실측: 시나리오 I 재검증에서 잠금 첫 행동·폴링 유지·스킬 경유·브랜치 네이밍·승인 게이트
  5개 항목 통과. 결함 1건 — 착수 시 티켓이 "해야 할 일"에 그대로 남음(착수 이동 스펙 누락).
- 수정: 잠금 직후 "해야 할 일"→"진행 중" 이동 규칙 추가("진행 중" 컬럼 없으면 기록 후 진행,
  모호하면 질문). acceptance-test I 기준 갱신, 리포트 `docs/test/2026-08-05.md` 작성.
