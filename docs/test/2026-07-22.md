# 테스트 리포트 — 2026-07-22

## 코드 분석 요약

- 대상: agent-project-kit 1.1.0 (`ea81667`, `1e8f67a`). 신규 기능은 공유 지침 문서
  라이프사이클(init 인터뷰 생성/adopt 병합), 사용자 스킬 팬아웃(`agent-kit-skill-sync`),
  manifest schema v1→v2 업그레이드.
- 자동 검증이 불가능한 대화형 플로우(수용 테스트 시나리오 B/D/E/F — `docs/acceptance-test.md`)
  가 이번 수동 테스트의 대상이다. 기계 판정 시나리오 A/C/G는 자동 회귀 테스트 78개가 덮는다.

## 테스트 계획

| 계층 | # | 테스트 항목 | 테스트 방법 | 기대 결과 | 선정 이유 |
|---|---|---|---|---|---|
| 자동 회귀 | 1 | 설치 격리·guard·마이그레이션·제거 | `python3 -m unittest discover -s tests` | 78개 전체 OK | 기계 판정 전 범위 |
| E2E(수동) | 2 | 시나리오 F: handoff 왕복 | Claude Code에서 handoff 작성 → Codex 블라인드 질문("직전 세션에서 무슨 작업을 하다가 넘어왔고, 다음에 뭘 해야 하지?") | 경로 힌트 없이 직전 작업·상태·다음 행동 복원 | Codex 자동 발견이 지침 기반이라 실측 필요 |
| E2E(수동) | 3 | 시나리오 E 일부: 스킬 팬아웃 parity | 두 사본 diff | diff 0 | 팬아웃 규칙의 핵심 계약 |

자체 검토: 충분성 — 대화형 4개 시나리오 중 F를 이번에 실측, B/D/E는 부분 증거만 확보(아래
결과 참조). 안전성 — 테스트는 별도 저장소(`~/main/github/test-proj`)에서 수행, 프로덕션 영향
없음. 연쇄 커버리지 — handoff → Codex 재해석 → git 상태 대조까지 도구 전환 전체 경로를 검증.

## 테스트 수행 상세

- 환경: macOS, Codex v0.145.0(gpt-5.6-sol xhigh), Claude Code. 대상 저장소
  `~/main/github/test-proj` (main / HEAD `b23aaa9 init`).
- 절차: ① Claude Code 세션에서 init(AGENTS.md/CLAUDE.md 생성)과 tidy-commits 사용자 스킬
  생성 후 `agent-kit-handoff` 실행 ② 새 Codex 세션에서 파일 경로를 알려주지 않고 블라인드
  질문 ③ Codex transcript를 사람(및 킷 저장소 세션)이 판정.
- 판정 근거는 사용자가 제공한 Codex transcript 전문이다. 킷 저장소 세션에서 test-proj의
  파일을 직접 열어 재검증하지는 않았다(미실행).

## 테스트 결과

- **#1 자동 회귀 — 통과**: `Ran 78 tests in 92.169s — OK` (kit 저장소, 1.1.0 커밋 전 실행).
- **#2 시나리오 F(handoff 왕복) — 통과**:
  - Codex가 경로 힌트 없이 `.agent-project-kit/CONTEXT.md`/`HANDOFF.md`/`agent-kit-handoff`
    스킬을 정확한 경로로 탐색·낭독 — `AGENTS.override.md` 지침 기반 자동 발견 작동 실증.
  - 직전 작업(tidy-commits 양쪽 배포), branch/HEAD(`main`/`b23aaa9`), 검증 완료/미완료 구분,
    다음 행동을 handoff 기준으로 복원.
  - handoff 기록과 실제 git 상태의 차이(스킬 파일 untracked 기록 vs 실제 staged)를 재확인으로
    잡아내 교정 — "handoff를 맹신하지 않는다" 규칙 준수. 확인 중 파일 무수정 보고.
- **#3 시나리오 E 일부 — parity 통과, 실행 검증 미완**: 두 사본 `diff` 0 확인(Codex 실측).
  Codex에서의 스킬 실제 실행 검증은 미완료로 남음.
- **발견 결함 1건 (킷 템플릿 모호성)**: test-proj의 init 생성 `AGENTS.md`가 "킷 파일 커밋
  금지"를 "`.claude/`·`.agents/` 전체 커밋 금지"로 과잉 일반화 → HANDOFF의 "사용자 스킬은
  공유 자산으로 커밋" 방침과 충돌(Codex가 발견). 원인은 `AGENTS.template.md`에 킷 소유/사용자
  소유 경로 경계 미명시. **조치**: 템플릿 §3에 commit 경계 문구 추가, `agent-kit-init` 3단계에
  과잉 일반화 금지 지침 추가(이 리포트와 같은 커밋).
- **부분 증거만 확보(미판정)**: 시나리오 B(init 인터뷰 품질)·D(adopt 병합)는 이번 transcript에
  결과물 존재만 나타나고 절차(질문·승인 흐름)는 직접 관찰하지 못했다 — 미검증으로 남긴다.

## 종합 의견

- 도구 전환의 핵심 경로(handoff 작성 → Codex 자동 발견 → 사실 대조 복원)는 첫 실전에서
  설계 의도대로 동작했다. 특히 기록-실제 불일치 교정은 하네스 규칙이 실제로 행동을 바꾼 증거다.
- 위험: init이 생성하는 규칙 문구의 품질은 모델 재량에 좌우된다. 이번 결함처럼 경계가 모호한
  주제는 템플릿에 명문화하는 방식으로 하나씩 고정해야 한다(측정된 실패 기반 추가 원칙).
- 남은 검증: 시나리오 B(인터뷰 절차 관찰), D(adopt 병합 diff·승인 흐름), E의 Codex 실행
  검증, 그리고 업그레이드된 템플릿으로 재설치 후 신규 init 결과에 commit 경계가 반영되는지
  확인이 다음 수동 테스트 대상이다.
