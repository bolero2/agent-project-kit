---
name: my-skill-name
description: 이 스킬이 무엇을 하고 **언제 쓰는지** 한 문장. (Claude가 이 설명만 보고 자동 발동 여부를 판단하므로 트리거 상황을 구체적으로.)
---

# <스킬 이름>

## 언제 쓰나
- (예: "벤치마크 돌려줘" / 특정 디렉토리의 코드를 수정할 때 / 배포 전)

## 절차
1. (구체적 단계 — 실제 검증된 명령만)
2. ...

## 주의 (지뢰)
- (이 작업에서 밟아본 함정들)

<!--
사용법:
1. 폴더 복사:     cp -r .claude/skills/_template .claude/skills/<스킬이름>
2. 개명 → 활성화: mv .claude/skills/<스킬이름>/SKILL.template.md .claude/skills/<스킬이름>/SKILL.md
3. frontmatter의 name(소문자-하이픈)과 description(발동 트리거)을 채운다.

원칙:
- 파일명이 정확히 SKILL.md 인 디렉토리만 스킬로 등록된다. 이 템플릿을 SKILL.template.md 로
  둔 것은 의도적 — 템플릿 자체가 스킬 목록에 등록되어 매 세션의 컨텍스트를 오염시키는 것을 막는다.
- 스킬 = "이 프로젝트에서 반복되는 작업의 표준 절차" (예: 컨테이너 실행법, 결과 검증법, 배포 절차)
- 반복해서 같은 지시를 2번째 하게 되는 순간 스킬로 만든다 (토큰 절약의 핵심).
- 절차가 길면 보조 파일(스크립트·체크리스트)을 같은 폴더에 두고 SKILL.md에서 상대경로로 참조한다.
-->
